<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="zAppe.MainPage">

    <StackLayout>
        <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
            <Label Text="Welcome to Xamarin.Forms!" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>
        <Label FontSize="16" Padding="30,24,30,0">
            <Label.FormattedText>
                <FormattedString>
                    <FormattedString.Spans>
                        <Span x:Name="testing" Text=""/>
                    </FormattedString.Spans>
                </FormattedString>
            </Label.FormattedText>
        </Label>
        <Label x:Name="state" FontSize="Title"/>
        <Switch x:Name="theSwitch" Toggled="Switch_Toggled"/>
        <Button x:Name="ConnectButton"
                Text="Connect"
                Clicked="ConnectButton_Clicked"
                Margin="10" />
        <Label Text="Not Connected" x:Name="connectionStatus"/>
        
    </StackLayout>

</ContentPage>
